TODO:
- button component (to test mouse click)
- how to capture mouse scroll wheel events?
- how to implement resizing and drag'n'drop mouse actions? (uses press and hold button logic)
- more thorough testing of what happens when a component is larger than allowed
- user interaction framework (needed for windows, buttons, etc)
- layout managers (Row, Column, etc)
- more components (duh :P)
- decide whether to use DOM event names in UI or use own names
- refactor cell.py and classes.py into cell/* and classes/*?
- Do pythonic type hints
- rewrite GlyphPage to be less of a hack
- support for gz compression of fonts?
- bdf2lgf conversion tool
- test of fonts with depth > 1 (multicolour)
- switch to Pyodide (Pyscript is in "maintenance mode" now)
- pixelated replacement cursors?

DONE:
- mouse overlay surface [Idea tried out and skipped because it added more HTML complexity]
- make a decision on whether obj.a = 1 (which then uses property for control) or obj.set_a(1) should be used
  Answer: We always use set_a().
- mouse tracking
- basic event subscription for key presses (keydown/keyup)
- moving panels around (done by adjusting cellupdate.pos)
- add hex2pickle support tool (experimental wide glyph support)
- add .gz support to bdf2pickle support tool
- how should a component trigger a re-layout workflow?  For now we are defining that a component cannot trigger
  a re-layout internally; it can only happen through a call to screen.update().
- refactor CellUpdates to use dict API for adding CU objects so you don't get namespace clashes with methods
- make CellPosition take a Position object instead of x, y args
- border/margin/padding (mostly done, certain exotic LD combinations not supported -- yet)
- Containers should not use CellUpdates but just add to the Component CellUpdates
- Rename CellUpdateXXX to XXX and cleanup old code from cell.py
- always use named arguments for cells/component classes
- line drawing (need revisiting in the future)
- cleanup unused/obsolete files
- rename Frame -> Panel
- fix component blank space (fill) design
- test positioning of absolute lm
- pyscript 2026.7.3
- test render cell update lifecycles
- screen/frame paradigm
- Update to latest PyScript (2026.7.2)
